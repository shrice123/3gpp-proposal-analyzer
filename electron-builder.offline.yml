extends: electron-builder.online.yml
artifactName: 3GPP-Proposal-Analyzer-Offline-${version}-${os}-${arch}.${ext}
extraResources:
  - from: backend/dist/proposal-backend
    to: backend/proposal-backend
  - from: runtime-bundle
    to: runtime-bundle
    filter:
      - "**/*"

